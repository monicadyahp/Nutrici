<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/newwlogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/newwlogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nutlicii"
        tools:targetApi="31">
        <activity
            android:name=".UI.View.LandingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.View.HistoryActivity"></activity>
        <activity android:name=".UI.View.FoodAddActivity"></activity>
        <activity android:name=".UI.View.MainActivity" />
        <activity android:name=".UI.View.LoginActivity" />
        <activity android:name=".UI.View.RegisterActivity" />
    </application>

</manifest>